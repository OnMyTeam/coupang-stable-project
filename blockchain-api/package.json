{
  "name": "coupang-stable-blockchain-api",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js JSON-RPC server for Payment contract calls on a Tempo-compatible EVM network.",
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "node --check src/server.js && node --check public/app.js && node scripts/validate-payment-api.mjs"
  },
  "dependencies": {
    "dotenv": "^17.2.0",
    "ethers": "^6.15.0",
    "express": "^5.1.0"
  }
}
